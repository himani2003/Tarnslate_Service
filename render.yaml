services:
- type: web
  name: translate-service
  env: python
  region: oregon # Choose a region close to your users
  buildCommand: "pip install -r requirements.txt"
  startCommand: "uvicorn translate:app --host 0.0.0.0 --port 8000"
